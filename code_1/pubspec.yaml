name: code_1
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.1
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.2
  geolocator: ^9.0.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  firebase_core: ^2.30.1
  firebase_auth: ^4.17.9
  cloud_firestore: ^4.16.1
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
  fonts:
    - family: ComicSans
      fonts:
        - asset: fonts/comic_sans.ttf
